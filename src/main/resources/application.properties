# ============================
#   DATA SOURCE (MySQL)
# ============================
spring.datasource.url=jdbc:mysql://localhost:3306/hms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============================
#   HIBERNATE & JPA SETTINGS
# ============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ============================
#   FILE UPLOAD
# ============================
app.uploads.dir=${user.home}/hms/uploads/reports
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# ============================
#   JACKSON
# ============================
spring.jackson.serialization.fail-on-empty-beans=false

# ============================
#   EMAIL CONFIGURATION (GMAIL)
# ============================

# Your FROM address (sender)
app.mail.from=pankajregade2017@gmail.com

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pankajregade2017@gmail.com

# \u26A0\uFE0F IMPORTANT: Your Gmail App Password (16 chars)
spring.mail.password=swqp yosy lvje agjo

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

spring.mail.default-encoding=UTF-8

# Enable mail debugging (shows full SMTP logs)
spring.mail.properties.mail.debug=true
